# Local-only override — stubs out Umbrel's app_proxy so docker compose
# works outside the Umbrel OS. Not needed on actual Umbrel.
services:
  app_proxy:
    image: busybox
    command: ["true"]
    environment:
      APP_HOST: crisler-lgtm-stack_lgtm_1
      APP_PORT: 3000
