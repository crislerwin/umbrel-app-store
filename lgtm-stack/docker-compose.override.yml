# Local override – stubs out Umbrel's app_proxy for docker compose testing outside Umbrel OS.
services:
  app_proxy:
    image: traefik:v2.10
    command: ["--help"]
    environment:
      APP_HOST: lgtm-stack_lgtm_1
      APP_PORT: 3000
